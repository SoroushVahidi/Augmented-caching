# Tests

`pytest tests/ -v` from the repository root (see root `README.md`).

Tests cover policies, simulator behavior, dataset helpers, and selected experiment scripts. They do **not** replace checking canonical **`heavy_r1`** analysis artifacts for manuscript numbers.
