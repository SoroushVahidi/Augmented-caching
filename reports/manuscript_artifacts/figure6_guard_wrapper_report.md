# Figure: guard wrapper schematic — build report

## 1. Repository files inspected

| Area | Files |
|------|--------|
| Documentation | `docs/guarded_robust_wrapper.md` |
| Implementation | `src/lafc/policies/guard_wrapper.py` (`GuardWrapperPolicy`, `EvictValueV1GuardedPolicy`) |
| Existing manuscript figures | `scripts/paper/manuscript_figure_common.py` (`make_method_overview_two_panel_figure`, `_box_axes`, `_arrow`, `apply_manuscript_matplotlib_style`, `save_figure_pdf_png`) |
| Builder | `scripts/paper/build_kbs_main_manuscript_artifacts.py` (Figure~1 caption notes guard is optional / not in canonical `heavy_r1` bundle) |
| Output layout | `figures/manuscript/README.md`, `reports/manuscript_artifacts/latex_snippets/` |

## 2. Existing figure assessment

- **`figure1_method_overview`** (generated by the main builder): panel (b) includes a single stack line “Guarded fallback if unsafe” but does **not** explain detector logic, parameters, dual-shadow behavior, or fallback duration. **Not sufficient** as a dedicated guard/fallback figure for Related Work / Methods.
- **No other** dedicated guard schematic was found under `figures/manuscript/` in this workspace.

## 3. Figure numbering note

`scripts/paper/build_additional_eviction_value_figures.py` already emits **`figure6_regret_vs_top1_alignment`** as a supplemental “figure 6” stem. The guard schematic uses a **distinct basename** `figure6_guard_wrapper_evict_value_v1` so files are not overwritten. **Renumber** `\label{fig:...}` / caption text in your LaTeX if the venue’s figure sequence conflicts.

## 4. What was created (new)

| Deliverable | Path |
|-------------|------|
| Schematic generator | `make_guard_wrapper_schematic_figure()` in `scripts/paper/manuscript_figure_common.py` |
| Build driver | `scripts/paper/build_guard_wrapper_manuscript_figure.py` |
| Vector + raster | `figures/manuscript/figure6_guard_wrapper_evict_value_v1.pdf`, `.png` |
| LaTeX insertion | `reports/manuscript_artifacts/latex_snippets/figure6_guard_wrapper_snippet.tex` |
| Caption (markdown) | `reports/manuscript_artifacts/figure6_guard_wrapper_caption.md` |

**Regenerate:**

```bash
cd /path/to/Augmented-caching
python scripts/paper/build_guard_wrapper_manuscript_figure.py
```

## 5. Fidelity to code/docs

- **Early-return signal**, **sliding window** \(T\), **threshold** \(M\), **re-request within** \(W\), **guard duration** \(D\), and **fallback** \(\{\texttt{lru},\texttt{marker}\}\) match `docs/guarded_robust_wrapper.md` defaults and `GuardWrapperPolicy` / `EvictValueV1GuardedPolicy`.
- **Dual-shadow advancement** and **visible cache follows active mode** are stated explicitly (from `on_request` in `guard_wrapper.py`).
- **Caption/snippet** repeats the repo’s conservative framing: practical layer, **not** a competitive robustness theorem (`docs/guarded_robust_wrapper.md` “Faithfulness statement”).

## 6. Quality limitations / uncertainty

- **Trigger vs.\ same-step eviction (code nuance):** `GuardWrapperPolicy.on_request` applies the base eviction on the step that crosses the suspicious threshold, then sets `_guard_remaining = guard_duration` so subsequent steps use the fallback shadow. The schematic text compresses this to “next D requests” level; tighten caption if reviewers require cycle-accurate wording.
- **Schematic only:** no timing diagram, no empirical trigger-rate plot (would need run artifacts / new experiments).
- **Defaults inlined in figure text** (W, T, M, D): if the manuscript discusses other CLI values, regenerate or edit the figure source strings in `make_guard_wrapper_schematic_figure()`.
- **Typography:** uses matplotlib sans-serif at manuscript rcParams; final venue may require font embedding checks (PDF already sets `pdf.fonttype=42` via shared style).
- **Canonical KBS `heavy_r1`:** quantitative bundle still does **not** include guarded A/B metrics (`build_kbs_main_manuscript_artifacts.py` notes); this figure supports **explanation** only.

## 7. LaTeX integration (repo-side only)

- Snippet uses `\includegraphics[width=\columnwidth]{...}`; for two-column `figure*`, switch to `width=0.92\textwidth` or similar in your LaTeX wrapper.
- Requires `graphicx`; `hyperref`/`url` if using `\path{...}` in caption as written.
