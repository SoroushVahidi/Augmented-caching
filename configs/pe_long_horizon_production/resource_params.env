# PE long-horizon production resource parameters.
#
# Values marked __TBD__ or __WAIT_FOR_1288047__ MUST NOT be filled in until
# Wulver job 1288047 (the cap256 timing probe) has COMPLETED and its
# measured Elapsed/MaxRSS/output-bytes have been substituted into the
# runtime model in docs/pe_long_horizon_design_notes.md.
#
# scripts/pe_long_horizon/launch_guard.py refuses to render the production
# sbatch template (or submit anything) while any placeholder below remains.

# --- measured, from job 1287838 (cap32 preflight, COMPLETED 0:0) ---
CAP32_PREFLIGHT_JOB=1287838
CAP32_ELAPSED=00:20:09
CAP32_MAX_RSS_MB=698
CAP32_OUTPUT_BYTES=8133338142
CAP32_FAMILIES_IN_RUN=5
CAP32_CAPACITY=32

# --- pending, from job 1288047 (cap256 timing probe, single family cloudphysics) ---
CAP256_PROBE_JOB=1288047
CAP256_ELAPSED=__WAIT_FOR_1288047__
CAP256_MAX_RSS_MB=__WAIT_FOR_1288047__
CAP256_OUTPUT_BYTES=__WAIT_FOR_1288047__
CAP256_FAMILY_IN_RUN=cloudphysics
CAP256_CAPACITY=256

# --- production resource request (to be derived from the above once known) ---
CPUS_PER_TASK=__TBD__
MEMORY_PER_TASK=__TBD__
WALLTIME_PER_TASK=__TBD__
ARRAY_CONCURRENCY=__TBD__

# --- production array shape (fixed by the manifest, not resource-dependent) ---
ARRAY_SPEC=0-19
NUM_PRODUCTION_TASKS=20
NUM_LOGICAL_CELLS=60
