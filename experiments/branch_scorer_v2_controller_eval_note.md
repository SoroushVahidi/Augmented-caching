# branch_scorer_v2 controller-level pilot evaluation note

This is the **first controller-level check** of the v2 learned branch scorer inside the pilot branch-allocation loop.

The v2 scorer used here was trained on **continuation-value approximations** (not direct end-task reward labels).

This readout is still a **lightweight pilot diagnostic**, not a final paper-level claim.
