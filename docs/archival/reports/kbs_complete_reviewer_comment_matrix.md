# Complete reviewer comment matrix (rebuilt from real comments, 2026-06-19)

**Source of truth:** `reports/kbs_real_reviewer_comments.md` — the verbatim
AE / Reviewer #2 / Reviewer #3 text supplied by the user in this session.
This file replaces the prior (2026-06-19, earlier the same day) 13-row
version that was built from a 12-13 item category-level paraphrase, before
the real comments were available. That older version, and the differently-
structured draft of the same filename on the unmerged
`kbs-revision-parallel-cleanup` branch, are both **superseded** — kept in
git history for provenance, not deleted, per instruction.

**Method note on "Be strict and honest":** every "Current status" cell below
is graded only against artifacts that actually exist in this repo today
(checked directly, not recalled). Where no artifact exists, the cell says so
plainly rather than describing a plan as if it were evidence. "Required
evidence" / "Required manuscript change" describe what would close the
comment, not what currently exists.

22 rows: AE (1) + Reviewer #2 Major Comments 1-3 (3) + Reviewer #3 Summary
(1) + Issues 1-7 (7) + Minor Problems 8-9 (2) + Recommended Revisions 1-8
(8) = 22.

**Companion drafting pass (2026-06-19, while `cap32_with_sieve_fifo`
runs).** `reports/kbs_manuscript_rebuttal_drafts/` now contains
results-independent draft text keyed to this matrix:
`response_paragraph_bank.md` (rebuttal-letter paragraphs for every row
below), `manuscript_insertions_draft.md` (manuscript-body text for 8
subsections), `title_and_contribution_reframing_options.md` (3 title
options + contribution bullets), and `docx_package_text_bank.md` (cover
letter/highlights/CRediT/declaration-of-interest/AI-disclosure text). All
four are **prepared, not final** — every claim dependent on cap128/256
or a baseline/fallback decision is an explicit placeholder. They do not
change the "Current status" column below by themselves; they are drafting
material to use once a row's blocker clears.

| Source | Comment summary | Required evidence | Required manuscript change | Required response text | Current status | Blocker | Priority |
|---|---|---|---|---|---|---|---|
| **AE** | Major concerns: no end-to-end miss-ratio eval, limited differentiation from prior learned-caching methods, insufficient baselines, unvalidated fallback, no overhead analysis; replay-horizon choice and label-construction scalability need deeper justification. | All of the evidence required by R2-MC1/MC2/MC3 and R3-Issue1-7 below — the AE summary is a roll-up, not a separate ask. | All changes required by the items it rolls up. | A short cover-letter-style paragraph mapping each AE concern to the specific reviewer item(s) and the corresponding response section. | **Not started.** No cover-letter text drafted yet (`kbs_docx_submission_package_report.md` confirms no revision-specific cover letter exists in any format). | Depends on all items below being resolved first (it's a summary). | High (do last, after the items below) |
| **R2-MC1** | Replay horizon H is manually specified; H=4 performs best but no principled justification, sensitivity analysis, theory, or adaptive mechanism is given. Unclear if optimal H is workload/cache-size/dataset-specific. | A horizon sensitivity analysis across H∈{4,8,16} (already run offline) reported with discussion of *why* H=4 wins, ideally per-trace-family, plus either a theoretical argument or an explicit acknowledgment that H is a tuned hyperparameter. | New subsection or expanded ablation discussion in the manuscript's model-selection section; possibly a workload-conditioned horizon breakdown (ties to R3-Minor9/Rec8). | "We investigated H∈{4,8,16}... H=4 is selected because... we acknowledge this is an empirically tuned choice and add the following sensitivity analysis..." | **Drafted with existing evidence.** `reports/kbs_horizon_h4_revision_strategy.md` now extracts the per-family validation-regret breakdown already sitting in `analysis/evict_value_wulver_v1_train_metrics_heavy_r1.json`, shows H=4 winning within every covered family, and provides the theoretical-intuition / reviewer-response text. Remaining open pieces: per-capacity analysis and the known 5-of-7-family validation-coverage caveat. | Writing only for immediate use; optional extra inference pass later if a per-capacity breakdown is desired. | High — now answerable much more strongly than before without new heavy compute |
| **R2-MC2** | Counterfactual replay labeling for every eviction candidate at every full-cache miss is computationally expensive; manuscript lacks discussion of preprocessing time, label-generation complexity, training scalability. | Quantitative offline-cost numbers: dataset-build wall-clock, size, label-generation complexity class. | New paragraph/table in Methods or Limitations reporting offline cost. | "Dataset construction for the full 7-trace × 4-capacity × 3-horizon sweep took ~10.3 hours wall-clock, producing 96GB across 662 shards. Per-model training took ~7-8 minutes..." | **Drafted with measured evidence.** `reports/kbs_overhead_manuscript_text_draft.md` now turns the measured numbers from `reports/kbs_overhead_and_scalability_evidence.md` into manuscript-ready and reviewer-response-ready text. | None unless a later pass wants a separate controlled online timing micro-benchmark. | High — answerable today with existing evidence, no new compute needed |
| **R2-MC3** | Results focus on offline metrics (regret, Top-1 agreement) that don't necessarily translate to online performance; need more end-to-end evals against strong baselines across workloads/capacities/prediction-quality regimes; guarded-fallback effectiveness not sufficiently demonstrated. | The canonical end-to-end miss-ratio sweep (cap32/64/128/256 × 7 traces × policies) plus a dedicated fallback-triggered-vs-not comparison. | Table 3 / Figs. 2-3 (currently `NOT_VERIFIED` stubs per `kbs_stale_artifact_refresh_plan.md` §6) need real numbers; a fallback-specific subsection. | Cannot be drafted with real numbers yet — placeholder only. | **Partially available, cap128 running.** Corrected cap32 and cap64 chunks both complete (8 policies incl. SIEVE/FIFO-Reinsertion, 56/56 rows each): `evict_value_v1` loses to LRU on 6/7 families at both capacities (−4.84% mean misses at cap32, −2.26% at cap64 — gap narrowing), also loses to SIEVE (−2.92%→−0.13%) and FIFO-Reinsertion (−4.77%→−2.19%). Analysis: `reports/kbs_cap32_with_sieve_fifo_result_analysis.md`, `reports/kbs_cap64_result_analysis.md`. `cap128_with_sieve_fifo` launched 2026-06-20 (tmux, local/cloud machine), same 8-policy set, not yet complete. cap256 not launched. No dedicated fallback-ablation artifact exists. | **Blocker**: cap128 completion + cap256 (pending review of cap128 result, per `reports/kbs_after_cap64_decision_memo.md`) + fallback ablation. | High, but genuinely gated on compute — see `kbs_after_cap64_decision_memo.md` and `reports/kbs_cap128_with_sieve_fifo_launch_report.md` |
| **R3-Summary** | Method is coherent but the empirical evidence is almost entirely offline target-quality metrics, not end-to-end cache-miss-ratio improvements; hedging language weakens the contribution. | Same as R2-MC3 (end-to-end results) plus a hedging-reduction editing pass. | Same Table 3/Figs. 2-3 fix; a prose tightening pass (ties to R3-Rec4/Rec6). | Roll-up; same caveat as the AE row — answerable only after the items below are answered. | **Not started as a standalone response** — depends on R3-Issue1 and R3-Rec1/Rec4/Rec6. | Same as R2-MC3/R3-Issue1 (canonical sweep) plus a writing pass. | High (synthesis item, do after Issue1-7) |
| **R3-Issue1** | No end-to-end cache miss ratio results; for a caching paper the primary metric must be downstream performance (miss ratio/hit rate) vs. strong baselines. | Canonical multi-capacity policy-comparison CSV (cap32+64+128+256, merged). | Table 3, Figs. 2-3. | Placeholder only until the sweep exists. | **2 of 4 capacity chunks done (corrected canonical); cap128 running.** `cap32_with_sieve_fifo` and `cap64_with_sieve_fifo` both complete: 56/56 rows each, 8 policies incl. SIEVE/FIFO-Reinsertion, `evict_value_v1` −4.84% vs LRU at cap32, −2.26% at cap64. `reports/kbs_cap32_with_sieve_fifo_result_analysis.md`, `reports/kbs_cap64_result_analysis.md`. `cap128_with_sieve_fifo` launched 2026-06-20 in tmux (`reports/kbs_cap128_with_sieve_fifo_launch_report.md`), same 8-policy set, not yet complete. cap256 not launched. Merged CSV still missing. | cap128 completion, then human decision on cap256 (`reports/kbs_after_cap64_decision_memo.md`). | Highest |
| **R3-Issue2** | Insufficient differentiation from HALP (NSDI '23), which already learns candidate-level preferences from future re-access outcomes and evicts the least-preferred candidate — operationally very close to this paper's proposal. Needs articulation of what the finite-horizon replay target adds, ideally with direct empirical comparison. | Either (a) a faithful HALP reimplementation with empirical numbers, or (b) a clear written differentiation plus an explicit, honest statement that empirical HALP comparison is out of scope. | Expanded Related Work / differentiation paragraph; Limitations sentence if (b) is chosen. | "Unlike HALP, which learns a pairwise preference signal from realized future re-access outcomes, our finite-horizon replay target explicitly quantifies downstream miss-count harm under counterfactual eviction... We did not reimplement HALP empirically in this revision because [reason]; we instead provide this analytical differentiation." | **Source-verified, still citation-only.** `reports/kbs_halp_fifo_source_verification.md` now re-verifies HALP against the NSDI'23 paper and Google research note, confirms `song2023halp` is already present in both the extracted manuscript bibliography and `refs/related_work_table6.bib`, and concludes that faithful empirical HALP reproduction is not realistic before 2026-07-08. | Baseline-scope decision only: accept positioning-only treatment or choose a much larger reimplementation effort. | High — reviewer named this baseline explicitly and it's the most direct "insufficient differentiation" ask |
| **R3-Issue3** | Missing comparisons to modern lightweight algorithms: SIEVE (NSDI '24), FIFO-Reinsertion, and others. | SIEVE and FIFO-Reinsertion implementations wired into the canonical pipeline with heavy_r1 numbers. | New Table 2 rows (policy roster) and Table 3 columns for SIEVE/FIFO-Reinsertion. | Placeholder only until implemented and run. | **Canonical cap32 and cap64 numbers available; cap128 running.** cap32: SIEVE 35,314 mean misses (−1.86% vs LRU), FIFO-Reinsertion 34,688 (−0.06% vs LRU). cap64: SIEVE 34,363 (−2.12% vs LRU), FIFO-Reinsertion 33,672 (−0.07% vs LRU). Both in `..._cap32_with_sieve_fifo.csv` / `..._cap64_with_sieve_fifo.csv` (56/56 rows each). `cap128_with_sieve_fifo` launched 2026-06-20 with the same SIEVE/FIFO-Reinsertion columns, not yet complete. cap256 still absent. | cap128 completion, then human decision on cap256; cap32+cap64 partial answer is draftable today. | Highest — cap32/cap64 addressable; cap128/256 still needed |
| **R3-Issue4** | Title promises "robust learning-augmented caching" but the paper itself admits it has not demonstrated robust end-to-end performance — a self-admitted empirical weakness undermining the contribution. | The same end-to-end results as R3-Issue1, which would either support or require softening the title/framing. | Either strengthen the empirical section (R3-Issue1) enough to earn the title's claim, or reframe the title/abstract/contributions to match what's actually demonstrated (ties to R3-Rec4). | Cannot be finalized until the canonical sweep result is known — if `evict_value_v1` continues to underperform LRU on most families (as cap32 already shows on 6/7), the honest response may be a title/scope change, not a defense. | **Substantially informed; cap128 running to test capacity sensitivity further.** Corrected cap32 and cap64 confirm `evict_value_v1` loses to LRU (−4.84%→−2.26%), SIEVE (−2.92%→−0.13%), and FIFO-Reinsertion (−4.77%→−2.19%) on aggregate, with the gap narrowing but no strict wins on most families yet. Reframing still indicated; see `reports/kbs_after_cap64_decision_memo.md`. `cap128_with_sieve_fifo` launched 2026-06-20 to get a third capacity data point before finalizing the framing decision. | Framing/scope decision; cap128 could further test capacity sensitivity but is unlikely to fully overturn the cap32/cap64 pattern. | High — reframing should proceed in parallel with any compute decision |
| **R3-Issue5** | Computational cost not addressed: per-miss feature construction + model inference for every cached item; no wall-clock analysis, no complexity comparison to O(1) baselines (LRU, SIEVE), no production-overhead discussion. | Code-verified complexity class + measured wall-clock numbers, ideally a controlled per-capacity timing benchmark. | New overhead subsection/table near the method description or in Limitations. | "`evict_value_v1` performs O(capacity) model-inference calls per cache miss (one per resident candidate), confirmed at `src/lafc/policies/evict_value_v1.py:179-200`, versus O(1) for LRU (`OrderedDict.popitem`)... measured wall-clock across existing runs is [X]; a dedicated controlled benchmark across capacities is [planned/in progress]." | **Drafted with code-verified complexity and measured offline times.** `reports/kbs_overhead_manuscript_text_draft.md` now separates measured evidence (dataset build, training) from code-verified asymptotics and explicitly states that a controlled online timing benchmark still does not exist. | Optional small timing benchmark if a cleaner latency table is later desired; no blocker for an honest manuscript/rebuttal draft today. | High — the asymptotic answer is already available and should be used |
| **R3-Issue6** | The fallback mechanism is unvalidated and oversold as a contribution; if not empirically validated it should not be listed as a main contribution. | A dedicated fallback-triggered-vs-disabled ablation with real numbers. | Either: validate the fallback with new evidence and keep it as a contribution, or demote it to an implementation detail / remove the claim (ties to R3-Rec5). | Cannot be drafted until the validate-or-demote decision is made (see `kbs_revision_evidence_ledger.md` §14 item 7, forward-referencing `kbs_before_cap64_baseline_decision_memo.md`). | **No fallback-specific ablation artifact exists anywhere in the repo** — confirmed absent during the overhead/baseline evidence searches in this and the prior session. | A scoping decision (validate vs. remove) plus, if "validate" is chosen, new (small) compute for the ablation. | High — reviewer explicitly says don't claim it as a contribution if unvalidated; leaving this unresolved risks a direct credibility hit |
| **R3-Issue7** | Single authorship combined with reliance on AI tools raises questions about validation depth, code correctness, result verification; multi-institutional/multi-author validation would help. | Not a technical-evidence ask — no artifact closes this. | Possibly a methods/acknowledgments statement about validation practices (e.g., test coverage, reproducibility steps taken). | A short, honest paragraph describing what validation steps were taken (e.g., the sanity-audit cross-check in `kbs_validation_sanity_audit.md`, unit tests, code review practices) without overclaiming multi-author review that didn't happen. | **Not started.** No such statement drafted. This is the one comment in the set that cannot be "resolved" with evidence, only addressed candidly in prose. | None (zero-compute, pure writing) — but requires an honest framing decision from the author/user, not something this audit can decide alone. | Medium — low effort to address, but sensitive; worth deciding wording deliberately rather than rushing |
| **R3-Minor8** | Excessive verbosity/repetition: Sections 1.1/1.2 overlap substantially; Sections 3.4 and 4.1-4.2 repeat methodological caution many times. | A line-by-line redundancy pass across the named sections. | Cut/merge Sections 1.1-1.2; trim repeated hedging in 3.4/4.1-4.2 (ties to R3-Rec6's 30-40% length target). | N/A (editing task, not a response-letter claim beyond "addressed"). | **Not started.** No editing pass has been done on `main.tex` in this repo session. | None — pure writing, zero-compute, can be done anytime. | Medium — easy win, but lower scientific stakes than Issue1-6 |
| **R3-Minor9** | Missing workload-specific analysis; given known workload dependence in caching, aggregate-only results are insufficient. | Per-trace-family breakdown of whatever metrics are reported (offline ablation and/or end-to-end miss ratios). | New workload-breakdown table/figure (ties to R3-Rec8). | "We report per-workload-family results in Table/Figure X, showing [pattern]." | **Available for cap32 and cap64; cap128 running.** `kbs_cap32_policy_comparison_report.md` §5 and `reports/kbs_cap64_result_analysis.md` §4 already have per-trace breakdowns for both completed capacities (7 families each); `cap128_with_sieve_fifo` is running and will provide the cap128 breakdown once complete. No equivalent breakdown exists yet for the offline ablation (H/model selection) or for cap256. | Canonical sweep (for the full per-capacity version) — but a per-trace cap32+cap64 table could be drafted today. | Medium-high — partially answerable today, fully answerable only after the sweep |
| **R3-Rec1** | Provide end-to-end miss ratio results. | Same as R3-Issue1. | Same as R3-Issue1. | Same as R3-Issue1. | Same as R3-Issue1 — corrected cap32 and cap64 done (56/56 rows, 8 policies each); `cap128_with_sieve_fifo` running (launched 2026-06-20); cap256 pending. | cap128 completion, then cap256 human decision. | Highest |
| **R3-Rec2** | Add direct comparisons to HALP and SIEVE. | Same as R3-Issue2 (HALP) + R3-Issue3 (SIEVE/FIFO-Reinsertion). | Same as those rows. | Same as those rows. | HALP: source-verified, positioning-only. SIEVE/FIFO: **canonical cap32 and cap64 numbers exist** (see R3-Issue3); `cap128_with_sieve_fifo` running for the cap128 column; cap256 still absent. | HALP baseline decision; cap128 completion + cap256 decision for the remaining multi-capacity SIEVE/FIFO columns. | Highest |
| **R3-Rec3** | Report computational overhead. | Same as R3-Issue5. | Same as R3-Issue5. | Same as R3-Issue5. | Same as R3-Issue5 — now drafted in manuscript-ready form, with the "no controlled online timing benchmark yet" caveat stated explicitly. | Optional small, scoped timing benchmark only. | High |
| **R3-Rec4** | Reduce hedging or reframe the paper's scope. | An editorial review of hedging language across the manuscript, cross-referenced against whatever the canonical sweep ends up showing. | Targeted rewrites in Sections 1.1/1.2 (ties to R3-Minor8), 3.4, 4.1-4.2, and the abstract/contributions list if scope is reframed. | Depends on the canonical sweep result (R3-Issue1) to know whether to reframe or defend. | **Not started.** No hedging-reduction pass done yet. | Writing-only, but the *direction* (defend vs. reframe) depends on the canonical sweep. | High — directly tied to whether the paper's core claim survives the full sweep |
| **R3-Rec5** | Validate the fallback mechanism or remove it. | Same as R3-Issue6. | Same as R3-Issue6. | Same as R3-Issue6. | Same as R3-Issue6 — no fallback ablation artifact exists. | Validate-or-demote decision, then (if validate) new small-scale compute. | High |
| **R3-Rec6** | Shorten the manuscript by 30-40%. | A word-count audit against the current ~8,919-word manuscript (per `kbs_docx_submission_package_report.md`'s prior-audit figure) and a concrete cut list. | Section-by-section edits, primarily 1.1/1.2 and 3.4/4.1-4.2 (R3-Minor8) plus general trimming elsewhere. | N/A (editing task). | **Not started.** No cut list or word-count-reduction pass exists yet. | None — pure writing, zero-compute. | Medium — best done together with R3-Rec4/Minor8 in one pass, after content (not just length) decisions are settled |
| **R3-Rec7** | Investigate why H=4 works best. | Same as R2-MC1. | Same as R2-MC1. | Same as R2-MC1. | Same as R2-MC1 — offline ablation data exists, no deeper sensitivity/theory discussion written yet. | Writing, ties to R3-Minor9/Rec8 for the workload-conditioned version. | High |
| **R3-Rec8** | Provide workload-specific breakdowns. | Same as R3-Minor9. | Same as R3-Minor9. | Same as R3-Minor9. | Same as R3-Minor9 — cap32 and cap64 both have per-trace breakdowns; `cap128_with_sieve_fifo` running; full version needs the canonical sweep. | Canonical sweep for the full version; cap32+cap64 partial version is draftable today. | Medium-high |

## Cross-check against the baseline-gap analysis

R3-Issue2/Issue3/Rec2 (HALP, SIEVE, FIFO-Reinsertion) match, almost exactly,
the baseline classification already done in `reports/kbs_baseline_gap_action_plan.md`
in the prior phase — that plan was built from a category-level "AE item 11"
paraphrase and is now confirmed directionally correct by the real text:
HALP and SIEVE are named explicitly. FIFO-Reinsertion is also named, and
the newer `reports/kbs_halp_fifo_source_verification.md` pass now resolves
the working interpretation to the CLOCK / Second-Chance family baseline
used as SIEVE's direct contrast in the NSDI'24/S3-FIFO literature.
PARROT and Mockingjay are **not named anywhere in the real
comments** — confirming the plan's earlier judgment that no action is
needed for those two beyond the existing citation.

## What changed from the superseded 13-row version

- Labels: "AE item 1-13" (category paraphrase) → the real 22 source labels
  (AE, R2-MC1-3, R3-Summary, R3-Issue1-7, R3-Minor8-9, R3-Rec1-8).
- The old "Reviewer #2/#3 mapping: Not determinable" column is gone — every
  row now has a real, known source.
- Several items that were previously merged into one paraphrased row (e.g.
  baseline gaps) are now split into their real, separate granularity
  (Issue2 vs. Issue3 vs. Rec2 are related but distinct asks).
